name: Generate Podcast Feeds
on: 
  push: 
    branches: 
    - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v5
      - name: Run Feed Generator
        uses: StevanSkrbic/podcast-generator@main
